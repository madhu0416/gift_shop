import Link from 'next/link';
import { Header, Footer, MobileNav } from '@/components/Site';
import { Section, SectionHeading, CircleRail, ProductGrid } from '@/components/Blocks';
import { weddingCategories, products, weddingCultures } from '@/lib/data';
import { ResponsiveImage } from '@/components/ResponsiveImage';
export default function CulturePage({params}:{params:{culture:string}}){const culture=params.culture.replaceAll('-',' '); const title=culture.replace(/\b\w/g,c=>c.toUpperCase()); return <><Header/><main><section className="collection-hero"><ResponsiveImage name="wedding" alt={`${title} wedding`} priority/><div className="collection-hero-copy"><span className="eyebrow">Culture-wise wedding</span><h1>{title} Wedding Gifts</h1><p>Discover a culturally inspired collection of wedding gifts, hampers, keepsakes and celebration essentials. Culture tags are independent from state tags, so the same product can belong to multiple wedding journeys.</p><Link href="/wedding" className="btn-secondary">All Wedding Collections</Link></div></section><Section tone="pink"><SectionHeading title="Wedding Categories" subtitle={`Shop the ${title} edit`}/><CircleRail items={weddingCategories}/></Section><Section><SectionHeading title={`Popular ${title} Picks`} subtitle="A curated starting point"/><ProductGrid items={products.slice(4,12)} cols={6}/></Section><Section tone="blue"><SectionHeading title="Other Wedding Cultures" subtitle="Explore another tradition"/><CircleRail items={weddingCultures}/></Section></main><Footer/><MobileNav/></>}
