import Link from 'next/link';
import { Header, Footer, MobileNav } from '@/components/Site';
import { Section, SectionHeading, ProductGrid, ImageCardRail } from '@/components/Blocks';
import { comboCards, products } from '@/lib/data';
import { ResponsiveImage } from '@/components/ResponsiveImage';
export default function ComboDetail({params}:{params:{slug:string}}){const slug=params.slug; const item=comboCards.find(x=>x.href?.endsWith(slug))||comboCards[0]; return <><Header/><main><section className="collection-hero"><ResponsiveImage name={item.image||'luxuryHamper'} alt={item.title} priority/><div className="collection-hero-copy"><span className="eyebrow">Giftly curated combo</span><h1>{item.title}</h1><p>{item.subtitle}. Build a complete gifting experience with coordinated products, optional personalisation and delivery-aware inventory.</p><Link href="/wedding/combos" className="btn-secondary">All Wedding Combos</Link></div></section><Section tone="cream"><SectionHeading title="What's Inside" subtitle="A visual merchandising preview"/><div className="feature-grid"><div className="feature-card"><ResponsiveImage name="bride" alt="Core item"/><div><h3>Core gift</h3><p>A hero product chosen for the occasion.</p></div></div><div className="feature-card"><ResponsiveImage name="giftBox" alt="Personalised add-on"/><div><h3>Personalised add-on</h3><p>Name, photo or message options.</p></div></div><div className="feature-card"><ResponsiveImage name="luxuryHamper" alt="Celebration add-on"/><div><h3>Celebration add-on</h3><p>Hampers, sweets or premium treats.</p></div></div><div className="feature-card"><ResponsiveImage name="family" alt="Home add-on"/><div><h3>Useful add-on</h3><p>Home, lifestyle or utility essentials.</p></div></div></div></Section><Section><SectionHeading title="Recommended Products" subtitle="Products that can form this combo"/><ProductGrid items={products.slice(0,8)} cols={6}/></Section><Section tone="pink"><SectionHeading title="Explore More Combos"/><ImageCardRail items={comboCards}/></Section></main><Footer/><MobileNav/></>}
