import Link from 'next/link';
import { Header, Footer, MobileNav } from '@/components/Site';
import { Section, SectionHeading, CircleRail, ProductGrid } from '@/components/Blocks';
import { weddingCategories, products, states } from '@/lib/data';
import { ResponsiveImage } from '@/components/ResponsiveImage';
export default function StatePage({params}:{params:{state:string}}){const state=params.state.replaceAll('-',' '); const title=state.replace(/\b\w/g,c=>c.toUpperCase()); return <><Header/><main><section className="collection-hero"><ResponsiveImage name="weddingCeremony" alt={`${title} wedding`} priority/><div className="collection-hero-copy"><span className="eyebrow">State-wise wedding</span><h1>{title} Wedding Gifts</h1><p>Shop a regional wedding edit built around gifting, celebration, home, personalisation and premium wedding moments.</p><Link href="/wedding" className="btn-secondary">Explore All States</Link></div></section><Section><SectionHeading title="Shop This Wedding Edit" subtitle="Categories designed for complete wedding gifting"/><CircleRail items={weddingCategories}/></Section><Section tone="pink"><SectionHeading title="Popular Picks" subtitle={`Featured gifting ideas for ${title}`}/><ProductGrid items={products.slice(4,12)} cols={6}/></Section><Section tone="blue"><SectionHeading title="More States"/><div className="chip-row">{states.map(s=><Link className="chip" key={s} href={`/wedding/state/${s.toLowerCase().replaceAll(' ','-')}`}>{s}</Link>)}</div></Section></main><Footer/><MobileNav/></>}
